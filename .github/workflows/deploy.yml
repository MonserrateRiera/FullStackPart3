name: testing deployment with heroku


on:
    push:
      branches:
        - main
jobs:
    deploy:
      runs-on: ubuntu-latest
  
      steps:
        # Checkout the code
        - name: Checkout code
          uses: actions/checkout@v3
  
        # Log in to DockerHub
        - name: Log in to DockerHub
          uses: docker/login-action@v2
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_TOKEN }}
  
        # Build and push the Docker image
        - name: Build and push Docker image
          run: |
            docker build -t mrieramar993/phonebook:latest .
            docker push mrieramar993/phonebook:latest
  
        # Deploy to Heroku

        # - name: Deploy to Heroku
        #   uses: akhileshns/heroku-deploy@v4.1.6
        #   with:
        #     heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
        #     heroku_app_name: tu-app-heroku
        #     heroku_email: ${{ secrets.HEROKU_EMAIL }}
        #     docker_image: tu-usuario-dockerhub/tu-repo:latest